

var mathObject = {
    num1:0,
    operation: '+', 
    num2:0
}

function addNumToTextx(num) {
    if(document.getElementById('myText')as HTMLInputElement).value !=0; {
       var (document.getElementById('myText')as HTMLInputElement).value =  (document.getElementById('myText')as HTMLInputElement).value + num;
    }
    else   {
        var (document.getElementById('myText')as HTMLInputElement).value = num;
       

    }
}
 function clear1x(){
    var (document.getElementById('myText')as HTMLInputElement).value = "";
 }
 function operationx(ope){
     var mathObject.num1 = document.getElementById('myText').value;
     mathObject.operation = ope;
     var document.getElementById('myText') mathObject = {
    num1:0,
    operation: '+', 
    num2:0
}

function addNumToText(num) {
    if(document.getElementById('myText').value !=0) {
        document.getElementById('myText').value = document.getElementById('myText').value + num;
    }
    else {
        document.getElementById('myText').value = num;
       

    }
}
 function clear1(){
    document.getElementById('myText').value = "";
 }
 function operation(ope){
     mathObject.num1 = document.getElementById('myText').value;
     mathObject.operation = ope;
     document.getElementById('myText').value = 0;
     
 }
 function calculate(){
    mathObject.num2 = document.getElementById('myText').value;
    if(mathObject.operation == '+'){
        document.getElementById('myText').value = parseInt(mathObject.num1) + parseInt(mathObject.num2);
        
    }
    else if(mathObject.operation == '-'){
        document.getElementById('myText').value = parseInt(mathObject.num1) - parseInt(mathObject.num2);
        
    }
    else if(mathObject.operation == '/'){
        document.getElementById('myText').value = parseInt(mathObject.num1) / parseInt(mathObject.num2);
        
    }
    else if(mathObject.operation == '*'){
        document.getElementById('myText').value = parseInt(mathObject.num1) * parseInt(mathObject.num2);
        
 }
 else if(mathObject.operation == '%'){
    document.getElementById('myText').value = parseInt(mathObject.num1) % parseInt(mathObject.num2);
    
 }
 else if(mathObject.operation == '='){
    document.getElementById('myText').value = parseInt(mathObject.num1) % parseInt(mathObject.num2);
    
 
 }

} value = 0;
     
 }
 function calculatex(){
    var mathObject.num2 = document.getElementById('myText')as HTMLInputElement).value;
    if(mathObject.operation == '+'){
        document.getElementById('myText')as HTMLInputElement;value = parseInt(mathObject.num1) + parseInt(mathObject.num2);
        
    }
    else if(mathObject.operation == '-'){
        document.getElementById('myText')as HTMLInputElement;value = parseInt(mathObject.num1) - parseInt(mathObject.num2);
        
    }
    else if(mathObject.operation == '/'){
        document.getElementById('myText')as HTMLInputElement;value = parseInt(mathObject.num1) / parseInt(mathObject.num2);
        
    }
    else if(mathObject.operation == '*'){
        document.getElementById('myText')as HTMLInputElement;value = parseInt(mathObject.num1) * parseInt(mathObject.num2);
        
 }
 else if(mathObject.operation == '%'){
    document.getElementById('myText')as HTMLInputElement;value = parseInt(mathObject.num1) % parseInt(mathObject.num2);
    
 }
 else if(mathObject.operation == '='){
    document.getElementById('myText');var mathObjectx = {
    num1:0,
    operation: '+', 
    num2:0
}

function addNumToText(num) {
    if(document.getElementById('myText')as HTMLInputElement;value !=0; {
        document.getElementById('myText')as HTMLInputElement;value = document.getElementById('myText')as HTMLInputElement).value + num;
    }
    else {
        document.getElementById('myText')as HTMLInputElement;value = num;
        

    }
}
 function clear1(){
    document.getElementById('myText')as HTMLInputElement;value = "";
 }
 function operation(ope){
     mathObject.num1 = document.getElementById('myText')as HTMLInputElement;value;
     mathObject.operation = ope;
     document.getElementById('myText')as HTMLInputElement;value = 0;
     
 }
 function calculate(){
    mathObject.num2 = document.getElementById('myText')as HTMLInputElement;value;
    if(mathObject.operation == '+'){
        document.getElementById('myText')as HTMLInputElement;value = parseInt(mathObject.num1) + parseInt(mathObject.num2);
        
    }
    else if(mathObject.operation == '-'){
        document.getElementById('myText')as HTMLInputElement;value = parseInt(mathObject.num1) - parseInt(mathObject.num2);
        
    }
    else if(mathObject.operation == '/'){
        document.getElementById('myText')as HTMLInputElement;value = parseInt(mathObject.num1) / parseInt(mathObject.num2);
        
    }
    else if(mathObject.operation == '*'){
        document.getElementById('myText')as HTMLInputElement;value = parseInt(mathObject.num1) * parseInt(mathObject.num2);
        
 }
 else if(mathObject.operation == '%'){
    document.getElementById('myText')as HTMLInputElement;value = parseInt(mathObject.num1) % parseInt(mathObject.num2);
    
 }
 else if(mathObject.operation == '='){
    document.getElementById('myText')as HTMLInputElement;value = parseInt(mathObject.num1) % parseInt(mathObject.num2);
    
 
 }

}value = parseInt(mathObject.num1) % parseInt(mathObject.num2);
    
 
 }

}